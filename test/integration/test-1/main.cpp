#include <iostream>
#include <stdlib.h>
#include <SDL.h>
using std::string;
using std::cout;
using std::endl;


// Very important to use "int main(int argc, char **argv), as sdl_main references it's own int main() -> or so that's the thought"
int main(int argc, char **argv) {
    system("pause");

    return EXIT_SUCCESS;
}
